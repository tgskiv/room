
var mappingManager = require('../../rooms');

var ListenerOnline = {
    onMessage: function (connection, message) {
        
        
        return true;
    }

};

module.exports = ListenerOnline;